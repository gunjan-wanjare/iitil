"use client";

/**
 * Optional thin overlay shell — IntroProvider already renders Loader + FloatingLogo.
 * Kept for Crediple architecture parity / future layering (e.g. vignette).
 */
export default function IntroOverlay() {
  return null;
}
